<?xml version="1.0" encoding="utf-8"?>
<ContentDialog
    x:Class="MicaForEveryone.App.Views.AddProcessRuleContentDialog"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MicaForEveryone.App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="_Add Process Rule"
    PrimaryButtonText="_Add rule"
    CloseButtonText="_Close"
    DefaultButton="Primary"
    IsPrimaryButtonEnabled="{x:Bind ViewModel.IsAddButtonEnabled, Mode=OneWay}"
    PrimaryButtonCommand="{x:Bind ViewModel.AddRuleCommand}">

    <ContentDialog.Style>
        <Style TargetType="local:AddProcessRuleContentDialog" BasedOn="{StaticResource DefaultContentDialogStyle}" />
    </ContentDialog.Style>

    <AutoSuggestBox PlaceholderText="_Enter a process name" TextChanged="AutoSuggestBox_TextChanged" Text="{x:Bind ViewModel.ProcessName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" ItemsSource="{x:Bind ViewModel.Recommendations, Mode=OneWay}" />

</ContentDialog>
