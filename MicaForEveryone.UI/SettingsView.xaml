<UserControl
    x:Class="MicaForEveryone.UI.SettingsView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MicaForEveryone.UI"
    xmlns:mux="using:Microsoft.UI.Xaml.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="500"
    d:DesignWidth="400">
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <ScrollViewer Padding="12">
            <StackPanel Spacing="8">
                <mux:Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Header="General">
                    <StackPanel Spacing="6">
                        <CheckBox Content="Reload config on changes" IsChecked="{x:Bind ViewModel.ReloadOnChange, Mode=TwoWay}" HorizontalAlignment="Stretch" />
                        <Border Height="1" Background="{ThemeResource SystemControlPageBackgroundBaseLowBrush}" />
                        <CheckBox Content="Run on startup" HorizontalAlignment="Stretch" />
                    </StackPanel>
                </mux:Expander>
                <mux:Expander HorizontalAlignment="Stretch" HorizontalContentAlignment="Stretch" Header="Global Rule">
                    <StackPanel Spacing="6">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Titlebar Color:" HorizontalAlignment="Stretch" />
                            <ComboBox Grid.Column="1" HorizontalAlignment="Stretch">
                                <ComboBoxItem Content="Default" />
                                <ComboBoxItem Content="System" />
                                <ComboBoxItem Content="Light" />
                                <ComboBoxItem Content="Dark" />
                            </ComboBox>
                        </Grid>
                        <Border Height="1" Background="{ThemeResource SystemControlPageBackgroundBaseLowBrush}" />
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="200" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <TextBlock Grid.Column="0" Text="Backdrop Type:" HorizontalAlignment="Stretch" />
                            <ComboBox Grid.Column="1" HorizontalAlignment="Stretch">
                                <ComboBoxItem Content="Default" />
                                <ComboBoxItem Content="None" />
                                <ComboBoxItem Content="Mica" />
                                <ComboBoxItem Content="Acrylic" />
                                <ComboBoxItem Content="Tabbed" />
                            </ComboBox>
                        </Grid>
                        <Border Height="1" Background="{ThemeResource SystemControlPageBackgroundBaseLowBrush}" />
                        <CheckBox Content="Extend Frame Into Client Area" />
                    </StackPanel>
                </mux:Expander>
                <TextBlock Text="About this app" Style="{ThemeResource SubtitleTextBlockStyle}" />
                <TextBlock Text="Mica for Everyone v0.0.0.0" />
                <HyperlinkButton Content="Github" NavigateUri="https://github.com/minusium/MicaForEveryone" />
            </StackPanel>
        </ScrollViewer>
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Content="Close" Style="{ThemeResource AccentButtonStyle}" Margin="8" />
        </Grid>
    </Grid>
</UserControl>
