<UserControl x:Class="MicaForEveryone.UI.LogsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="using:MicaForEveryone.UI"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="using:MicaForEveryone.Models"
             d:DesignHeight="300"
             d:DesignWidth="400"
             mc:Ignorable="d">
    <ListView ItemsSource="{x:Bind ViewModel.Logs}">
        <ListView.ItemTemplate>
            <DataTemplate x:DataType="models:LogEntry">
                <StackPanel Orientation="Horizontal">
                    <TextBlock Text="{x:Bind TimeStamp}" />
                    <TextBlock Text="{x:Bind Success}" />
                    <TextBlock Text="{x:Bind WindowTitle}" />
                    <TextBlock Text="{x:Bind WindowClass}" />
                    <TextBlock Text="{x:Bind ProcessName}" />
                </StackPanel>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</UserControl>
